---
tags: [阶段小结, 考试质量分析系统]
date: 2026-08-20
---

# 阶段小结（2026-08-20）

> 时间：2026-08-19 ~ 2026-08-20

## 一、完成内容

### 1. 续接 8/11 功能轮次收尾（8/19）
- 修复 `tests/test_api.py` 未完成测试（语法错误，pytest 无法收集）
- 修复 `exams.py` create/update 丢失 `parse_uuid(grade_id)` 导致的 UUID 写入错误
- 修复前端 12 处 TS 类型错误（Analysis / ClassAnalysis / StudentTracking / Exams）
- 补齐 `report.py` 错题集、错题集导出、练习生成 3 个端点（服务层与前端早已就绪）
- 补齐文档中声明但缺失的 `GET /health/llm`
- `requirements.txt` 补充 `reportlab`、`python-docx`（已在用但未声明，修复部署隐患）

验证结果：后端 **40/40 测试通过**；前端 `vue-tsc` + `vite build` 通过；新端点冒烟测试通过（错题集 200 / Word 导出 200 / 练习生成 8 题 / health 200）。

### 2. 功能设计定稿（8/20）
- 赋分制分析：`docs/功能设计_赋分制与知识点智能录入.md`
- 知识点智能录入：同上文档
- 设计已同步保存至 Obsidian 知识库（Codex/Projects/考试质量分析系统/）

## 二、待办（已完成）

- 修复 alembic 迁移链（`b7dee85ed6bc_initial_migration.py` 已被删除，链断裂）→ 已修复并重建迁移链
- 实施 M1 数据层（迁移 005：scoring_schemes / score_lines / knowledge_sources + 加列）→ 已完成
- 提交本轮全部改动（8/11 功能轮次 + 8/19 修复 + 8/20 设计文档）→ 已完成（commit aa4923e / d5fd9b4）

## 三、实施落地（8/20 续：赋分制 + 知识点智能导入）

### M1 数据层（commit d5fd9b4）
- 重建 alembic 迁移链，新增迁移 005：`scoring_schemes` / `score_lines` / `knowledge_sources` 三张表；`exam_subjects` 加 `scoring_type/scheme_id/conversion_mode`；`scores` 加 `converted_score/converted_source`；`subject_knowledge_points` 加 `source_id/origin`
- 种子：内置 3 套赋分方案（新高考3分一段 / 浙江1分一段 / 山东8级）；知识点预设改为空库填充且只清理 origin=preset

### M2 赋分后端（本轮）
- `ScoreConversionService`：累计百分比定等级 + 段内等比例插值，并列同分，auto/manual 双模式（manual 不覆盖官方赋分）
- 方案 API：`GET/POST/PUT/DELETE /scoring-schemes`（内置不可删）
- 考试配置：`GET/PUT /exams/{id}/scoring-config`；分数线：`GET/POST /exams/{id}/score-lines` + `POST /exams/{id}/score-lines/import`（Excel 批量）
- 成绩：`POST /scores/{exam_subject_id}/convert`（手动重算 force 参数）；批量保存支持 `converted_score` 列并在 auto 模式下自动换算
- 分析：`GET /analysis/exam/{id}?score_mode=raw|converted|both|auto`（auto 按数据完整度降级）；`/line-stats`（总分线/单科线/双上线 + 各班分解）；`/one-point-table`（一分一段表）

### M3 赋分前端（本轮）
- `Analysis.vue`：口径切换（自动/原始分/赋分/双轨）、上线统计卡片、一分一段表、both 模式赋分列
- `Exams.vue`：赋分配置对话框（计分方式/方案/自动手动）、分数线对话框（增删 + Excel 批量导入）
- 类型与 API：`types/index.ts` + `api/index.ts` 新增方案/分数线/口径统计封装

### M4 知识点后端（本轮）
- `KnowledgeImportService`：文本抽取（txt/docx/xlsx/pdf，PDF 无文本层走 OCR 适配器占位）、层级规则引擎（章节词/中文序号/括号中文/数字层级/括号数字，按编号体系栈建树）、AI 抽取（LLM JSON Schema + 失败回退规则）、预览去重标记、同名合并事务入库
- API：`GET /knowledge-points/import/template.xlsx`、`POST /import/excel|text|ai`（→预览）、`POST /import/preview`（确认入库）、`GET/DELETE /knowledge-points/sources`
- `KnowledgeOcrAdapter` 抽象接口预留（OCR_PROVIDER 配置位）

### M5 知识点前端（本轮）
- `KnowledgePoints.vue`：智能导入向导（目录文本/Excel 模板/AI 抽取三种模式）、树形预览（新增/库中已有合并/批次重复标记）、确认导入、来源管理

## 四、验证与后续

- 后端测试：**51/51 通过**（新增赋分 6 项 + 知识点导入 5 项）
- 前端：`vue-tsc` 类型检查 + `vite build` 构建通过
- 后续：内置赋分方案数值以各省官方文件核对；扫描版 PDF 接入 OCR 提供方；ScoreEntry 手工赋分列可视化编辑
