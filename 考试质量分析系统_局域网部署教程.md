---
tags: [部署, 教程, 考试系统]
created: 2026-07-26
---

# 考试质量分析系统 — 局域网部署教程

## 一、部署方案概述

采用 **方案三：局域网服务器部署**，将系统安装在一台 Windows 办公电脑上，同一局域网内的所有教师通过浏览器访问使用。

### 架构说明

教师电脑 (浏览器) --> 服务器电脑 (http://IP:8000) --> FastAPI + SQLite

---

## 二、硬件与网络要求

### 服务器电脑要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Windows 10/11 64位 | Windows 11 |
| CPU | 双核 2.0GHz | i5 及以上 |
| 内存 | 4GB | 8GB |
| 硬盘 | 500MB 可用空间 | 1GB 以上 |
| 网络 | 有线/无线局域网 | 千兆以太网 |

### 教师机要求

- 任何带浏览器的设备均可，无需安装任何软件
- 访问 http://服务器IP:8000

---

## 三、详细部署步骤

### 第一步：设置服务器固定 IP

1. 按 Win+R → 输入 
cpa.cpl → 回车
2. 右键当前网络连接 → 属性
3. 双击「Internet 协议版本 4 (TCP/IPv4)」
4. 选择「使用下面的 IP 地址」，填写网络信息
5. 点击确定保存

### 第二步：构建前端静态文件

powershell:
  cd C:\Users\lijun\OneDrive\文档\考试质量分析系统\frontend
  npx vite build

### 第三步：配置后端托管前端

修改 backend/app/main.py，在末尾添加：

  from fastapi.staticfiles import StaticFiles
  import os
  frontend_dist = os.path.join(os.path.dirname(os.path.dirname(__file__)), "frontend", "dist")
  if os.path.exists(frontend_dist):
      app.mount("/", StaticFiles(directory=frontend_dist, html=True), name="frontend")

### 第四步：启动服务

  cd C:\Users\lijun\OneDrive\文档\考试质量分析系统\backend
  .venv\Scripts\uvicorn app.main:app --host 0.0.0.0 --port 8000

(--host 0.0.0.0 是关键，局域网内其他电脑才能访问)

### 第五步：放行防火墙端口

管理员 PowerShell：
  netsh advfirewall firewall add rule name="考试分析系统" dir=in action=allow protocol=TCP localport=8000

### 第六步：验证部署

- 服务器上访问 http://localhost:8000
- 其他电脑访问 http://服务器IP:8000

### 第七步：注册为 Windows 服务（开机自启）

下载 NSSM：https://nssm.cc/download

管理员 PowerShell：
  .\nssm.exe install ExamAnalysis

配置：
- Application Path: ...\.venv\Scripts\uvicorn.exe
- Arguments: app.main:app --host 0.0.0.0 --port 8000
- Startup directory: backend 目录

管理服务：
  net start ExamAnalysis
  net stop ExamAnalysis
  sc query ExamAnalysis

---

## 四、安全注意事项

- 部署后修改默认密码（admin / admin123）
- 定期备份 backend/exam_analysis.db

---

## 五、常见问题

Q: 其他电脑无法访问？
1. ipconfig 确认服务器 IP
2. ping 服务器IP 检查连通性
3. 检查防火墙是否放行 8000 端口
4. 检查学校网络策略

Q: 数据库在哪里？
backend/exam_analysis.db，建议每日备份。
